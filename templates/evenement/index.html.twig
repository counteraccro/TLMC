{% extends 'base.html.twig' %}

{% block title %}Hello {{ controller_name }}!{% endblock %}

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>

<div class="example-wrapper">
    <h1>Hello {{ controller_name }}! ✅</h1>

    This friendly message is coming from:
    <ul>
        <li>Your controller at <code><a href="{{ 'src/Controller/EvenementController.php'|file_link(0) }}">src/Controller/EvenementController.php</a></code></li>
        <li>Your template at <code><a href="{{ 'templates/evenement/index.html.twig'|file_link(0) }}">templates/evenement/index.html.twig</a></code></li>
    </ul>
</div>

<div>
	
	{% for evenement in evenements %}
		Nom :
	    {{ evenement.nom }}
		<br /> Dates :
	    {{ evenement.dateDebut |date("m/d/Y H:i") }} => {{ evenement.dateFin|date("m/d/Y H:i") }}
		
		<br />Nombre participants max :
	    {{ evenement.nombreMax }}
		
		
		<br />Description :
	    {{ evenement.description }}
	
		
		<br />Type : 
		{{ evenement.type }}
		
		<br />Image : 
		<img src="{{ evenement.image }}" height="200" width="200"/>
		
		<br />Info comp :
	    {{ evenement.informationComplementaire }}
		
		<br />
	
		<hr>
	{% endfor %}
	

</div>
{% endblock %}
