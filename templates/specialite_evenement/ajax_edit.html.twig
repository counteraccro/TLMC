<div id="ajax_specialite_evenement_edit">
	{{ form_start(form) }}
	<div class="modal-header">
		<h5 class="modal-title">
			Modification du lien #{{ specialiteEvenement.id }}
		</h5>
		<button type="button" class="close" data-dismiss="modal"
			aria-label="Close">
			<span aria-hidden="true">&times;</span>
		</button>
	</div>

	<div class="modal-body">
	    {{ form_row(form.evenement) }}
		{{ form_row(form.specialite) }}
		{{ form_row(form.date) }}
		{{ form_row(form.statut) }}
	</div>
	
	<div class="modal-footer">
		{{ form_widget(form.save) }}
	</div>
	{{ form_rest(form) }}
	{{ form_errors(form) }}
	{{ form_end(form) }}
</div>

{% block javascripts %}
	<script type="text/javascript">
    
    	$('#modal_{{ type }}_global').modal({
    		   'show' : true,
    		   'backdrop' : 'static'
    	});
    	
		var url = '{{path('specialite_evenement_ajax_edit', {'id': specialiteEvenement.id, 'type': type }) }}';
		SpecialiteEvenement.EventEditSubmit(url);
		
	</script>
{% endblock %}